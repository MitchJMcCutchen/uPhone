import { ExpirationDirective } from './expiration.directive';

describe('ExpirationDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpirationDirective();
    expect(directive).toBeTruthy();
  });
});
